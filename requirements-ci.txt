# Minimal dependencies for deterministic tests. Model, UI, and live-API tests
# are deliberately excluded from the default CI job.
langgraph==1.1.9
langgraph-checkpoint-sqlite==3.0.3
langchain-core>=1.0.0
pydantic>=2.0.0
typing-extensions>=4.0.0
pytest>=8.0.0
